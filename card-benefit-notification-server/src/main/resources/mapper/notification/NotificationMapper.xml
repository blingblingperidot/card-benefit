<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cardbenefit.notification.mapper.NotificationMapper">

    <!-- FCM 토큰 조회 -->
    <select id="findFcmToken" parameterType="String" resultType="map">
        SELECT fcm_token
        FROM users
        WHERE user_id = #{userId}
    </select>

    <!-- 알림 이력 저장 -->
    <insert id="insertNotificationLog" parameterType="map">
        INSERT INTO notification_logs (log_id, user_id, benefit_id, status, created_at)
        VALUES (
            notification_logs_seq.NEXTVAL,
            #{userId},
            #{benefitId},
            'SENT',
            SYSTIMESTAMP
        )
    </insert>

</mapper>