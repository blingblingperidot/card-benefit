server.port=8082

spring.application.name=card-benefit-card-server

# Oracle DB 연결 설정
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:XE
spring.datasource.username=card_benefit
spring.datasource.password=abcd1234

# MyBatis 설정
# mapper-locations : XML 파일 위치
# type-aliases-package : DTO 패키지 위치
# map-underscore-to-camel-case : DB 컬럼명(snake_case) → Java 필드명(camelCase) 자동 변환
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.type-aliases-package=com.cardbenefit.card.dto
mybatis.configuration.map-underscore-to-camel-case=true

# JWT 설정
# secret : 토큰 서명에 사용하는 비밀키 (모든 서버 동일하게 유지)
# expiration : 토큰 만료시간 (86400000ms = 24시간)
jwt.secret=card-benefit-secret-key-must-be-at-least-256-bits-long
jwt.expiration=86400000

# Kafka 설정
# bootstrap-servers : Kafka 브로커 주소
# key-serializer : 메시지 키 직렬화 방식
# value-serializer : 메시지 값 직렬화 방식
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer