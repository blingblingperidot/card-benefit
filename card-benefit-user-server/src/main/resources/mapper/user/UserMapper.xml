<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cardbenefit.user.mapper.UserMapper">

	
	<!-- 회원가입 -->
	<insert id="insertUser" parameterType="map">
	    INSERT INTO users (user_id, email, password, nickname, created_at)
	    VALUES (#{userId}, #{email}, #{password}, #{nickname}, SYSTIMESTAMP)
	</insert>

    <!-- 이메일로 사용자 조회 -->
    <select id="findByEmail" parameterType="String" resultType="map">
        SELECT user_id, email, password, nickname, created_at
        FROM users
        WHERE email = #{email}
    </select>
   
	<select id="findById" parameterType="String" resultType="map">
	    SELECT user_id, email, password, nickname, usr_tp_cd, created_at
	    FROM users
	    WHERE user_id = #{userId}
	</select>
	
	<!-- 아이디 중복 확인 -->
	<select id="checkUserId" parameterType="String" resultType="map">
	    SELECT user_id
	    FROM users
	    WHERE user_id = #{userId}
	</select> 
	
<!-- FCM 토큰 업데이트 -->
<update id="updateFcmToken" parameterType="map">
    UPDATE users
    SET fcm_token = #{fcmToken}
    WHERE user_id = #{userId}
</update>	   

</mapper>